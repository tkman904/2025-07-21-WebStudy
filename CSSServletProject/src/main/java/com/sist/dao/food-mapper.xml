<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.dao.food-mapper">
<!-- 
  	SQL문장
  		<select>    : 검색 SELECT
  		<insert>    : 추가 INSERT
  		<update>    : 수정 UPDATE
  		<delete>    : 삭제 DELETE
  		<sql>       : 재사용 = 복잡한 SQL문장 여러번 반복 
  		<resultMap> : 조인시에 주로 사용
  			=> 컬럼명과 VO의 변수가 틀린 경우 =>
  			<result column="name" property="nickname">
 -->
</mapper>